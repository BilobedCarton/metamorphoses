import React from "react"
import { ProgramViewContainer } from "./components/ProgramViewContainer"

export const BehindTheScenesView = (props: any) => {
    return (
        <ProgramViewContainer>
            <div>
                Behind the scenes
            </div>
        </ProgramViewContainer>
    )
}